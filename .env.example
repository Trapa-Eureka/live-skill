# Copy to .env — never commit .env (guardrail 7, CLAUDE.md).
ANTHROPIC_API_KEY=
MODEL=claude-sonnet-4-5
GATE_THRESHOLD=0.9   # 0.5 이상 1 이하 — 하한은 정책(docs/DESIGN.md §7), 코드가 강제한다
QA_PER_SECTION=3
MAX_LLM_CALLS=300
