# Copy to .env — never commit .env (guardrail 7, CLAUDE.md).
ANTHROPIC_API_KEY=
MODEL=claude-sonnet-4-5
GATE_THRESHOLD=0.9   # between 0.5 and 1; the lower bound is policy (docs/DESIGN.md §7), enforced in code
QA_PER_SECTION=3
MAX_LLM_CALLS=300
